# Public packages
access=public

# Use provenance for supply chain security (Trusted Publisher)
provenance=true

# Package manager
package-manager-strict=true

# Registry authentication (will be set by GitHub Actions via NODE_AUTH_TOKEN)
//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}
